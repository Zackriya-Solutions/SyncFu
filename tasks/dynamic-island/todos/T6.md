# TODO T6 - Model B overflow: Rust-owned rank/dedupe/count + dumb renderer

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** D5 locked Model B as the ONLY overflow model. C1/A3 resolved ownership to guard G10
LETTER: the frontend store provably diverges from the manager (four divergence windows), so
rank/dedupe/cap/spotlight/count live in Rust and the frontend renders an authoritative snapshot.
A4: waiter-bearing notifications are NEVER merged (exit-code integrity).

**What:** manager.rs gains priority ranking, dedupe by `group ?? sender::title` with
waiter-exemption, 6-row cap, spotlight selection (top priority), count, and an island snapshot
builder; lib.rs emits `island:snapshot` (scoped emit_to("island")) on EVERY manager change; new
IslandList.tsx renders the snapshot DUMBLY (rows, stagger 40-60ms, bottom fade, scroll past
6/560px - enforce the 560 cap here with overflow containment, NOT the morph cap, per the T4b
review note); Island.tsx routes single-vs-list from the snapshot and signals pause/resume of
auto-dismiss while the list is open (the ONE frontend->backend round-trip G10 names); island.css
list rows + bottom fade per the approved mockup Model B anatomy. NO frontend rankDedupe module
(C1 supersedes).

**How:** Extend the existing tested manager core (it already owns active/queued/groups). Badge
width formula 26+8*(digits-1), caps display at "9+". Waiter-exemption: a notification with a
pending waiter keeps its own row/id (check the waiters registry state at snapshot build - decide
the exact mechanism against waiters.rs and document). The T4a interim ingestFilter stays as
defense-in-depth but the snapshot is the island's data source now - remove or bypass the store
derivation for list data (document which).

**Files:** `src-tauri/src/notification/manager.rs`, `src-tauri/src/lib.rs` (+ http.rs emit sites),
`src/components/island/IslandList.tsx` (new), `Island.tsx`, `src/styles/island.css`, tests + e2e.

**Dependencies:** T5a (merged - per-row actions), T5b (merged - lifecycle seam).

**progress.txt observations:** thought-ledger entries: store starvation interim (T4a F3), the 560
auto-height bypass (T4b review), history-unwired discovery (T10's problem - do not wire here).
manager.rs divergence windows W1-W4 are documented in 85-verify.md A3 - your snapshot design must
make them irrelevant (frontend never derives).

**Architectural alignment note:** guards G10 (Rust owns Model B), G11 (Models A/C rejected - a
test asserts their absence, closing FR-13). D5 numbers are locked: cap 6 rows/560px, stagger
40-60ms, critical first, auto-dismiss paused while open.

**F2 adjudication (binding, from T5a review):** the manager enriches emitted island payloads /
snapshot rows with `hasWaiter` (computed against the WaiterRegistry at emit time - NOT a
sender-settable payload field, G12 intact). Frontend suppression of auto-collapse/auto-dismiss is
scoped to `hasWaiter || critical`; non-wait actionable notifications follow the card's auto-dismiss
semantics. Unanswered island --wait resolving exit 2 via CLI timeout is RATIFIED behavior.

**Known unknowns:** waiter-exemption mechanism at snapshot build (adjudicate vs waiters.rs);
whether pause/resume needs a manager flag or frontend-only timer suspension (G10 names one
round-trip - decide, document).

**Acceptance:** Rust unit tests - ranking, dedupe key, waiter-exemption, cap, spotlight, badge
formula (2/10/100+ digits), snapshot determinism; C1 divergence guard test (island snapshot NOT
derived from the frontend store); Playwright - compact-grouped badge widths, expanded list 6-row
cap + scroll + fade + stagger vs the mockup grouped references; timers-pause-while-open test;
Models A/C absence test; all gates green.
