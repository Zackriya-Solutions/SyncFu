# TODO T10 - Docs / CLI help / API docs + compat closure + history wiring + PRD amendments

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** Every shipped flag/field/knob must be discoverable and honest (especially the capture
matrix). This task also closes two debts assigned by the run's thought ledger: (1) the
history-wiring discovery (historyStore.prependEntry has ZERO production callers - history is an
unwired shell for ALL presentations; wiring it at the shared ingest gives card + island history in
one move and closes R-HISTORY); (2) the six PRD amendments (90-synthesis section 5 tail) applied
as explicit edits.

**What (four workstreams):**
1. DOCS: README island section (send examples incl. --presentation island --wait, settings
   reference with all 13 knobs/ranges/defaults, notch-vs-float, Model B behavior); CLI help strings
   verified + island examples; HTTP/WS API docs gain the presentation field + island:snapshot event;
   the HONEST capture matrix (guaranteed macOS <= 14 + Windows 19041+; macOS 15+ best-effort with
   the no-public-API explanation; Linux unsupported). Island --wait semantics documented: unanswered
   -> exit 2 after --wait-timeout (RATIFIED, differs from card's frontend-dismiss exit 1).
2. HISTORY WIRING: prependEntry called at the shared useNotifications ingest (both windows/
   presentations; dedupe against double-ingest across windows - the overlay AND island windows both
   run the hook, so guard by window label or store identity - trace and decide); history-includes-
   island acceptance test; HistoryView renders island entries.
3. COMPAT TESTS: old payload without presentation renders top-right unchanged (exists from T1 -
   verify still green and extend to the HTTP boundary snapshot emit); --update on old clients.
4. PRD AMENDMENTS: apply the six items itemized in 90-synthesis section 5 to
   tasks/prd-dynamic-island.md as explicit edits (capture-scoping rewrite per A1/OQ-1, waiter-
   dedupe-exemption FR, lifecycle ratification OQ-2, dedupe-key wording C6, history-store
   correction C2, radius ranges OQ-4) + the T5a-review invariant-b rewording (island --wait
   unanswered = exit 2).

**Files:** README.md, cli help strings (cli/src/main.rs doc strings only - no behavior),
docs/ (API + settings reference; confirm layout in-task), src/hooks/useNotifications.ts +
src/stores/historyStore.ts wiring + tests, tasks/prd-dynamic-island.md.

**Dependencies:** T1/T5a/T5b/T6/T7a/T7b/T9 (all merged when dispatched).

**progress.txt observations:** the history discovery + F2/exit-2 ratification are in
collective_thoughts.txt and progress.txt; the em dash ban applies to ALL new doc text; no
AI-attribution anywhere.

**Architectural alignment note:** invariant (b) - docs never claim invisibility not delivered;
invariant (c) - docs match MERGED behavior (verify against code, not the plan); history wiring is
the ONE production-code workstream - keep it surgical (ingest-point only).

**Known unknowns:** docs dir layout; double-ingest guard design for history (decide, document).

**Acceptance:** docs reviewer checks every flag/field/knob against code; README example commands
actually run (execute them against a dev build's HTTP port or note why not); history test green;
all gates green.
