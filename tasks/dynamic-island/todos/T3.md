# TODO T3 - Island window plumbing + backend presentation routing

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** The island needs its own OS window (guard G1: dedicated window, capture scope isolated)
anchored top-center, and the backend must route by `presentation` so an island send never pops the
empty top-right panel (closes FR-3). G1 gate delivered the notch recipe; G2 decided the window
type; T1 delivered the routing field. All three are merged in your base.

**What:** New `src-tauri/src/overlay/island.rs` (ISLAND_LABEL, IslandEnvelope, pure
`calculate_island_anchor`, create/show/hide/reflow + `set_island_capture_protected` stub wiring),
new macOS-gated `src-tauri/src/overlay/notch.rs` (NotchGeometry from the G1 verdict recipe -
gates/G1-verdict.md has copy-paste code), module wiring in overlay/mod.rs, and lib.rs: create the
island hidden in .setup() after create_panel; branch notify() on payload.presentation to emit_to
the target window and show only it; route dismiss/update by the stored presentation; dismiss_all
broadcasts to both.

**How:** Mirror panel.rs structurally (NSPanel non-activating / all-Spaces / full-screen-auxiliary
on macOS via the same tauri_panel! pattern; always-on-top WebviewWindow elsewhere). Fixed expanded
envelope, frame NEVER resized during morph (D3). Idle click-through via ignore_cursor_events(true).
Monitor targeting: built-in notched display if present (safeAreaInsets().top > 0 iterating
screens(mtm), NEVER mainScreen(); per G1), else cursor monitor; non-notch -> floating capsule
fallback (~185x32 basis). objc2-app-kit becomes a real (target-gated) dependency now, main-thread
access via the existing run_on_main_thread pattern (panel.rs:196/229).

**Files:** `src-tauri/src/overlay/island.rs` (new), `src-tauri/src/overlay/notch.rs` (new,
macOS-gated), `src-tauri/src/overlay/mod.rs`, `src-tauri/src/lib.rs`, `src-tauri/Cargo.toml`
(promote objc2-app-kit to target-gated [dependencies]).

**Dependencies:** G1 (merged), G2 (merged), T1 (merged).

**progress.txt observations:** G1 measured 183pt on this machine; NSZeroRect (not None) on
non-notch; G2 confirmed protect-before-show ordering and NSPanel default sharingType=readOnly.
show_panel/hide_panel exist today and must NEVER touch the island (R-TWOWIN).

**Architectural alignment note:** guard G1 enforced here. A `card` send must behave byte-identically
to today (invariant f). Frontend App.tsx routing is T4a's job - this PR is backend-only; the island
window hosts the existing frontend which routes by label (a blank island window until T4a is
expected and acceptable, hidden by default).

**Known unknowns:** external non-notch display of a notched Mac (fallback capsule; record actuals);
exact envelope height for the fixed frame (use expanded max: expandedWidth 560 cap x maxExpH 560
basis from the settings schema; document the chosen numbers).

**Acceptance:** centered-anchor unit tests across 1080p / retina 2x / 4k 1.5x / secondary-offset
(mirror the 6 existing calculate_panel_position tests); integration test: card-only send emits only
to overlay, island send emits only to island and does not pop the panel; `cargo check --workspace`
+ `cargo test --workspace` green; existing panel tests untouched and green.
