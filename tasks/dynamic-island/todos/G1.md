# TODO G1 - GATE spike: notch-geometry access from Rust (verdict only)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** T3's notch-hugging anchor needs physical notch width from Rust. Wave V A2 de-risked this
to near-certain (objc2-app-kit 0.3.2 already in Cargo.lock with public NSScreen bindings); the gate
confirms on real hardware and hands T3 a zero-research start.

**What:** A written go/no-go verdict with a minimal proof snippet. Deliverables: (1)
`tasks/dynamic-island/gates/G1-verdict.md` (committed, docs-only) recording the exact API path,
main-thread requirements, measured values on this machine, non-notch semantics, and the T3 recipe;
(2) a throwaway probe under `src-tauri/examples/notch_probe.rs` (compiles, not shipped code).

**How (A2 tightenings, binding):** add `objc2-app-kit = "0.3"` as a DIRECT dependency (dev/example
scope acceptable for the spike); read `NSScreen.auxiliaryTopLeftArea` / `auxiliaryTopRightArea` /
`safeAreaInsets` ON THE MAIN THREAD (`MainThreadMarker` / `run_on_main_thread`); boring.notch
formula `notchWidth = screen.width - auxLeft - auxRight + 4`; explicitly record the objc2
non-optional NSRect vs Swift `NSRect?` nullability semantics observed on a non-notch display (this
machine's display situation must be recorded either way); fallback bar ~185x32.

**Files:** `src-tauri/Cargo.toml` (spike dep), `src-tauri/examples/notch_probe.rs` (new),
`tasks/dynamic-island/gates/G1-verdict.md` (new).

**Dependencies:** none.

**progress.txt observations:** tauri-nspanel already does main-thread AppKit work in this codebase
(lib.rs main-thread dispatch pattern exists - reuse it).

**Architectural alignment note:** PUBLIC NSScreen APIs only (guard G5: query attempted before any
fallback; no SkyLight/private APIs - notarization-safe).

**Known unknowns:** macOS version differences in auxiliary-area API; external non-notch display of
a notched Mac (record, do not solve).

**Acceptance:** probe compiles in the workspace toolchain and prints plausible geometry on this
machine (macOS 26.5); verdict file gives T3 a definitive go/no-go + recipe; `cargo check` green.
