# TODO T1 - Presentation field: tri-file plumbing + CLI flag (no rendering change)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** Every island task depends on the payload knowing its presentation. OQ-3 RESOLVED: field
name `presentation`, values `card | island`, default `card`, CLI `--presentation`. This PR makes
the field exist and survive transport; rendering is untouched until T4a.

**What:** Additive `Presentation` enum + field across the tri-file pattern + clap flag + client
population + HTTP mapping. Selecting `island` still renders the card today (documented in PR body).

**How:** Follow the existing enum pattern exactly (Priority/ActionStyle in the same files:
serde rename_all lowercase, FromStr with helpful error, Default impl). `#[serde(default)]` on the
payload field for R-OLDCLIENT back-compat. TS side: optional field + `Presentation` type;
`WindowLabel` union gains `"island"`. No geometry keys in the payload (guard G12).

**Files:** `cli/src/main.rs` (clap flag), `cli/src/types.rs` (enum + FromStr + NotifyRequest
field), `cli/src/client.rs` (populate), `src-tauri/src/server/http.rs` (NotifyRequest field +
payload mapping ~line 136), `src-tauri/src/notification/types.rs` (enum + `#[serde(default)]`
field), `src/types/notification.ts` (type + optional field + WindowLabel).

**Dependencies:** none (parallel with gates + harness).

**progress.txt observations:** types.rs files carry a "keep in sync" header comment - honor it;
existing serde round-trip tests in all three Rust type modules are the pattern to extend.

**Architectural alignment note:** guards G8 (not a theme overload), G9 (transported field, not
CLI-only), G12 (payload = 27 --s-* overrides only, no geometry). Existing overlay behavior must be
byte-for-byte unchanged.

**Known unknowns:** none material (name ratified by user).

**Acceptance:** serde round-trip + default-omitted tests in Rust (omitted field -> Card at every
boundary); CLI integration assertion that `--presentation island` posts `"presentation":"island"`
through the HTTP boundary; all existing Rust + Vitest suites pass; `cargo check`, `cargo test`,
`pnpm tsc --noEmit`, `pnpm test` green.
