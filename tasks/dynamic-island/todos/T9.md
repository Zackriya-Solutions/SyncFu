# TODO T9 - Capture-exclusion production wiring + honest per-OS status

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** OQ-1 RESOLVED to honest scoping: capture invisibility guaranteed on macOS <= 14 + Windows
19041+; macOS 15+ best-effort with honest "limited on this OS" status (NO auto-hide subsystem);
Linux toggle disabled + "Not supported on Linux". G2's verdict (gates/G2-verdict.md) proved the
mechanism and the tri-state; T7a shipped the hideFromScreenCapture setting. This task ships it.

**What:** Wire the toggle end to end with the tri-state status: island.rs applies protection at
window creation AND live on toggle (in-place, never rebuild - T7a already re-applies on toggle;
this task adds the STATUS surface and per-OS gating). New status API: a get_capture_status IPC
returning ON / BEST_EFFORT / UNSUPPORTED / UNKNOWN + reason string, derived from OS version
detection (macOS Darwin major < 24 -> ON when enabled; Darwin >= 24 -> BEST_EFFORT; Windows build
>= 19041 -> ON; < 19041 -> UNSUPPORTED; Linux -> UNSUPPORTED; undetectable -> UNKNOWN fail-safe
"assume visible"). NEVER derive status from sharingType read-back (G2: false safety). The flag is
still applied on macOS 15+ (harmless defense-in-depth) but status says BEST_EFFORT. TS mirror of
the status enum + store field so T7b can render it.

**How:** OS version via std/existing deps (macOS: Darwin kernel major from uname or sw_vers parse;
Windows: build number; prefer what the codebase/deps already expose - check tauri's os APIs before
adding anything). Keep G2's guards: apply-first ordering preserved (T3/T7a); protection scoped to
the island window only (G1 - the card window is never touched); public APIs only (R-MACOS-PRIVATE).

**Files:** `src-tauri/src/overlay/island.rs` (status derivation + get status fn),
`src-tauri/src/lib.rs` (get_island_capture_status command), `src/types/islandSettings.ts` +
`src/stores/islandSettingsStore.ts` (status mirror) + tests.

**Dependencies:** G2 (merged), T7a (merged).

**progress.txt observations:** G2 verdict: on 26.5 all capture funnels through TCC-gated SCK;
sharingType read-back INSUFFICIENT; NSPanel default readOnly; tri-state recipe in the verdict.
T7a: set_island_capture_protected(app, bool) already parameterized; toggle re-apply wired.

**Architectural alignment note:** invariant (b) macOS 15+ NEVER surfaces a silent green "hidden";
invariant (e) Linux never claims invisibility; invariant (f) overlay window unchanged. The
per-OS manual capture checklist is lane 06's release gate - this task ships the status logic and
unit tests for the derivation matrix, not the manual probes.

**Known unknowns:** exact OS-version API surface available without new deps (resolve in-task;
smallest footprint wins); Windows build detection is code-only here (no Windows machine) - unit
test the derivation function with injected versions.

**Acceptance:** Rust unit tests for the full derivation matrix (Darwin 23/24/25 x enabled/disabled,
Win build 19040/19041, Linux, unknown -> fail-safe UNKNOWN); toggle round-trip test (set false ->
protection re-applied false, status follows); TS store mirrors status; `cargo check/test
--workspace`, `pnpm tsc --noEmit`, `pnpm test` green; overlay untouched.
