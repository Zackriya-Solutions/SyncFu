# TODO G2 - GATE spike: capture exclusion on the island window type (live probe, verdict only)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** Wave V A1 proved macOS 15+ ScreenCaptureKit ignores sharingType/setContentProtected
(Apple DTS: no public API) and that property read-back gives FALSE safety. OQ-1 resolved to honest
scoping. T9 needs a real per-OS verdict from the exact window type we ship (tauri-nspanel NSPanel).

**What:** A written per-OS verdict at `tasks/dynamic-island/gates/G2-verdict.md` (committed,
docs-only) + throwaway probe code. On THIS machine (macOS 26.5, post-15 SCK world): create a
minimal tauri-nspanel window with `set_content_protected(true)`, then run a LIVE capture probe and
record whether the window appears. Read-back of sharingType is recorded but explicitly marked
insufficient. Windows/Linux sections are documented from evidence (A1 verdict + research), marked
"not probed on this machine".

**How:** Probe ideas (worker picks the simplest that works): `screencapture -x` region capture of
the window rect and pixel-compare; and/or a small `screencapturekit`-based capture via `swift`
one-liner script if available. Record BOTH the legacy-capture result and the SCK result. Reuse the
existing panel creation path (overlay/panel.rs macos branch) for fidelity to the shipped window
type. NSPanel collection-behavior (`full_screen_auxiliary`) interaction gets recorded.

**Files:** `tasks/dynamic-island/gates/G2-verdict.md` (new), throwaway probe (example bin or
scratch script referenced from the verdict; not shipped).

**Dependencies:** none.

**progress.txt observations:** overlay/panel.rs already creates the NSPanel via tauri_panel! macro
with non-activating config - the probe must use the same conversion to be valid.

**Architectural alignment note:** guards - never copy open-island `.readOnly`; apply-first
ordering (protect BEFORE first show); honest status tri-state (ON / UNSUPPORTED / UNKNOWN) is what
T9 will surface, per OQ-1 honest scoping (no auto-hide subsystem).

**Known unknowns:** whether macOS 26.5 SCK behaves like the documented 15.x behavior (RECORD the
actual result - this machine is the newest data point); scripting method for a headless SCK probe.

**Acceptance:** verdict file contains: exact API path used, live-probe methodology + result on
macOS 26.5 (screenshots or pixel-diff evidence), legacy-capture result, per-OS scope table
(guaranteed <= 14 + Windows WDA; macOS 15+ best-effort; Linux unsupported), and a go/no-go + recipe
for T9. `cargo check` stays green (probe code isolated).
