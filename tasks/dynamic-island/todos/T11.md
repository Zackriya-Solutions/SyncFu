# TODO T11 - End-to-end acceptance: the operator journey (final sign-off task)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** The end-to-end operator journey is its own acceptance item that no single task satisfies
(completeness gate: own every seam). This is the final sign-off before the integration PR.

**What:** Prove, with automation where possible and an honest recorded manual checklist where not:
(a) `syncfu send --presentation island --wait -a approve:Approve -a deny:Deny:danger` -> arrives
as an island -> approve resolves exit 0; deny path exit semantics; timeout exit 2. (b) 3+
concurrent island notifications -> Rust-owned Model B ranked list; each row's action resolves the
RIGHT waiter (A4 end-to-end). (c) capture-exclusion state recorded per the honest matrix (this
machine is macOS 26.5: record BEST_EFFORT status surfaced + flag applied; the guaranteed-tier
manual checks are a release-checklist artifact, not automatable here). (d) settings changes reflect
live mid-journey. (e) the existing top-right card journey still passes (regression).

**How:** tauri-driver does NOT support macOS - do not fight it. Compose the journey from the
layers that exist: spawn the REAL app binary (cargo build + run, or tauri dev) so the REAL HTTP
server + manager + windows run; drive it with the REAL CLI (the syncfu binary); assert exit codes
and HTTP state; for UI-layer assertions reuse the Playwright harness routes (already proven) plus,
where the real webview is needed, macOS screencapture of the island window region against
structural expectations (document TCC needs honestly if blocked - the G2 pattern). If spawning the
full app headlessly proves impossible in this environment, degrade honestly: automate the
CLI<->HTTP<->manager journey end-to-end (no webview), cover the UI layer via the harness suites,
and record the composition argument + a manual checklist for the human. NO production code changes;
bugs found here get reported, not fixed in this branch.

**Files:** `e2e/journey/` specs + runner scripts; `tasks/dynamic-island/RELEASE-CHECKLIST.md`
(the manual capture + cross-OS items, honest per-OS); no src/ changes.

**Dependencies:** ALL prior tasks merged.

**progress.txt observations:** the app has never been launched in this run (all testing layered);
budget real time for first-launch issues - report them as findings, do not fix. The visual-baseline
machine-sensitivity note (T5b/T6 reviews) belongs in the RELEASE-CHECKLIST as a CI-canonicalization
item.

**Architectural alignment note:** this task is EVIDENCE, not code. Its deliverable doubles as the
final PR's test-plan section.

**Known unknowns:** headless app spawn viability; TCC for any capture probe (degrade honestly).

**Acceptance:** journey suite green on this machine for the automatable path; RELEASE-CHECKLIST.md
complete with recorded results + remaining manual items; regression journey (card) green; all
gates green.
