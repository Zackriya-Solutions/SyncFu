# TODO T2 - Shape / spring / wall-inset port (pure frontend libs, no window plumbing)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** Every island renderer needs the D3 shape/motion primitives. The approved mockup rev 8
contains the reference vanilla-JS implementation; the plan (90-synthesis task 5) mandates PORT, not
redesign. Extracting the shared 27-override map stops card/island forking (C8).

**What:** Typed modules with ZERO Tauri imports: `src/lib/notchPath.ts` (notchPath + capsulePath,
radii 6/14 -> 19/24, clamps t<=min(W/4,H/4) b<=min(W/4,H/2)); `src/lib/spring.ts` (rAF damped
spring 220/25 container, 400/30 content, 260/18 pop, 1000/100 reduced-motion, eps=0.02 resting
guard, rounding guard on retargets, dispose() cancelling pending rAF); `src/lib/styleVars.ts`
(EXTRACT buildStyleVars + the --s-* map from NotificationCard.tsx so both renderers share one map;
NotificationCard switches to importing it - behavior byte-identical); plus a dev harness route the
Playwright harness can screenshot per shape state.

**How:** Source of truth = tasks/dynamic-island-mockup.html (notchPath ~line 750s, Spring class
~780s, --di-wall publication ~880s, hover 1.028/150ms). Resolve the mockup's own +margin drift
(+4/+5/+2 across content presets) to ONE documented rule per content type, and the 22-vs-24 float
wall-radius drift to ONE value - record both decisions in code comments and the PR body.
A6 binding: measure() one-shot, never per-frame, never unrounded-resize feedback.

**Files:** `src/lib/notchPath.ts` (new), `src/lib/spring.ts` (new), `src/lib/styleVars.ts` (new),
`src/components/overlay/NotificationCard.tsx` (extract-only edit), dev harness route (follow the
repo's existing entry pattern; e.g. a query-param branch in main.tsx or an e2e-served static page -
pick the smallest that Playwright can shoot), `e2e/` spec additions on the T0 config.

**Dependencies:** none hard; T0 harness is merged - use it for acceptance.

**progress.txt observations:** T0 established e2e/ + snapshotPathTemplate conventions (port 5199
python http.server); follow them. Vitest + Tauri mocks exist for unit tests.

**Architectural alignment note:** guards G2 (hand-rolled spring - no motion lib), G3 (SVG path, no
CSS mask), G4 (no canvas), G7 (spring, not linear tween). Invariant R-WALL: wall-inset math never
lets content cross the concave shoulder. Hover scale 1.028 top-anchored / 150ms delay lives here
(closes FR-6 gap). Modules must not import @tauri-apps/*.

**Known unknowns:** float-precision parity tolerance vs mockup (define a numeric tolerance in the
unit tests and record it); the +margin and 22/24 drift resolutions (decide, document, proceed).

**Acceptance:** unit tests for notchPath geometry (anchor cases from the D3 table incl clamps),
spring settle/reduced-motion/dispose, wall-inset rule, hover params; Playwright screenshots of the
harness route matching mockup shot-* within threshold; NotificationCard extraction proven
behavior-identical (existing vitest suite + overlay snapshots unchanged); `pnpm tsc --noEmit`,
`pnpm test`, `npx playwright test` green.
