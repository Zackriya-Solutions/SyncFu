# TODO T8 - Appearance (dark/light/auto) + float positions (incl bottom-center)

| Reference instruction (do not skip): Identify root cause first. Stay grounded. /think regression
| possibilities. Trace call paths. Verify reachability. Understand context, not just lines. Check
| references and usages. /simplify only after thorough /review. If clarity is missing in any area,
| capture the uncertainty here and resolve it during execution.

**Why:** The remaining D4 render behaviors, both user-requested: appearance dark/light/auto (light
restyles the expanded card + float pills; the macOS notch compact pill STAYS pure black - same rule
iOS applies) and float positions left/center/right/bottom-center (bottom-center ~12px above the
bottom edge, expands UPWARD, notch shape mirrored vertically when flush).

**What:** Light-theme surface tokens in island.css (from the approved mockup's light stage: surface
#f4f4f6-family, dark text ink ramp, hairline rgba(0,0,0,.10), float pill #e9e9ee; accent #0a84ff
reference); auto = prefers-color-scheme with live media-query listener; notchPath.ts gains the
vertical-mirror path for flush bottom-center; island.rs computes float anchor points for the four
positions (bottom-center anchors the window bottom edge; expansion grows upward via
transform-origin bottom + the morph's height growing from the bottom anchor); Island.tsx wires
appearance + position from the settings store.

**How:** Appearance affects the EXPANDED surface + float compact pills only; the notch-mode compact
pill fill stays literal #000000 (T4a invariant d - do not touch that constant path). Mirror path:
reuse notchPath's segment math with y-flip rather than a second generator (smallest change wins -
document approach). Positions are float-mode only (notch mode ignores/rejects, T7b handles the UI
side). Bottom-center: content grows upward - the fixed OS frame is already tall enough (600x560);
anchor content to the frame bottom in that mode.

**Files:** `src/styles/island.css`, `src/lib/notchPath.ts`, `src/components/island/Island.tsx`,
`src/lib/islandMorph.ts` (bottom-anchor awareness if needed), `src-tauri/src/overlay/island.rs`
(float anchors incl bottom-center window placement), tests + e2e snapshots.

**Dependencies:** T4a, T4b, T7a (all merged); T7b runs in parallel - coordinate via the settings
store contract only (no shared files with T7b except Island.tsx settings consumption - keep your
Island.tsx edits scoped to appearance/position wiring to minimize merge friction; the orchestrator
merges T7b first if both land together).

**progress.txt observations:** mockup rev 8 has the exact light tokens (t-light theme cell) and
the bottom-center behavior spec (playground caption: expands upward). calculate_island_anchor is
pure and unit-tested - extend the same test pattern for the four float positions.

**Architectural alignment note:** D4 notch-black rule (guard); D3 frame never animates (position
changes reposition the window instantly via reflow_island - not animated).

**Known unknowns:** exact light ink ramp values (pull from mockup .t-light block; document);
whether islandMorph needs bottom-anchor awareness or CSS alone suffices (decide, document).

**Acceptance:** Playwright snapshots - dark/light/auto expanded cards, light float pill, all four
float positions with bottom-center proven expanding upward (bottom edge stays put mid-morph);
notch-compact-stays-black-in-light test; anchor unit tests for four positions; gates green.
