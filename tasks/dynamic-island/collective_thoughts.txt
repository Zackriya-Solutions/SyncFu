# collective_thoughts.txt - single writer: orchestrator. Agents return cross-refs; orchestrator appends.

[from:orchestrator] [to:02-architecture] The approved mockup already contains working vanilla-JS
  reference code: notchPath() 8-segment Bezier generator with clamps, rAF damped-spring integrator
  (220/25, 400/30, 260/18, 1000/100), --di-wall live wall-inset content rule, Model B ranked-list
  logic with dedupe and cap. Architecture should PORT these, not redesign. (evidence: mockup rev 8)

[from:orchestrator] [to:02-architecture] [to:05-tasks] Two platform unknowns need EARLY GATES:
  (1) Tauri path to NSScreen auxiliary areas for notch width; (2) does set_content_protected(true)
  work on the tauri-nspanel NSPanel? If not, sharingType must be set via raw AppKit handle. Both
  are one-day spike tasks that de-risk everything downstream.

[from:orchestrator] [to:04-risk] Session lesson worth encoding as a risk: content-vs-shape geometry
  bugs recurred 3x in the mockup because verification checked box-vs-box alignment only. The
  implementation must carry the --di-wall content-inset invariant and the test strategy must assert
  content-vs-shape, not just element alignment.

[from:orchestrator] [to:06-test-strategy] Machine already has Playwright 1.61.1 + chromium + webkit.
  The mockup's shot-* ids and the audit probes from this session (wall-audit, corner zoom, console
  probe) are reusable seeds for the visual-regression harness.

[from:orchestrator] [to:01-prd] The user framing for customization: knobs are persistent app
  settings; senders NEVER control geometry. Keep FRs consistent with that split (payload = 27
  style overrides only).

# ---- Wave R harvest (orchestrator, post-R) ----

[from:04-risk] [to:ALL] GROUND-TRUTH CORRECTIONS to the context pack: (1) notification history is a
  FRONTEND store, not SQLite - correct any plan claim otherwise; (2) set_content_protected is unused
  in today's codebase; (3) NSWindow sharingType write is deprecated on macOS 14+ - capture strategy
  must use public APIs (feasibility input, not just risk).

[from:02-architecture] [to:ALL] Core recs: dedicated second window labeled "island" (App.tsx routes
  by label already); one new payload field presentation card|island default card via #[serde(default)]
  (backward compatible); port mockup JS into src/lib/{spring,notchPath,styleVars,islandRanking}.ts;
  settings = plain serde JSON island.settings.json (tauri-plugin-store NOT installed); manager stays
  source of truth for Model B; routing = emit_to per window + frontend filter safety net.

[from:03-alternatives] [to:ALL] Guard register G1-G12 exists (03-alternatives.md). Key: G1 never
  share window (capture leak), G2 motion lib only re-adjudicable for content crossfade never the
  frame, G5 notch query behind early gate with hardcoded/config Plan B pre-approved, G10 queue
  ownership stays in Rust manager, G11 overflow models A/C dead, G12 no per-notification geometry.

[from:05-tasks] [to:80-feasibility] Task plan is 13 tasks: G1/G2 spikes first, then tri-file field,
  window, port libs, island component, Model B, settings, capture PR, compat, e2e journey, docs.
  Provisional lifecycle default: arrive-expanded-then-collapse-to-pill; PRD must ratify.

[from:06-test-strategy] [to:02-architecture] [to:05-tasks] Implementation must expose test hooks:
  window.__islandSettled settle flag + --di-wall published on the real component (audit reads it
  live). Playwright harness lands BEFORE Tauri code as earliest green gate. macOS runner minutes are
  the scarce CI budget.

[from:01-prd] [to:ALL] PRD artifact: tasks/prd-dynamic-island.md (FR-1..FR-18 + glossary + OQ-1..9).
  Owns ratification of: presentation field name/default, lifecycle default, dedupe-by-group.

# ---- Wave A harvest (orchestrator, post-A) ----

[from:80-feasibility] [to:ALL] VERDICT: REVISE. C1 HIGH: Model B ownership contradiction - guard G10
  (Type-1, Rust manager owns ranked list) violated by architecture S7 + tasks T6 (frontend ranking
  module). Must resolve in synthesis; G10 wins unless re-adjudicated. C2: "SQLite history" persists
  in PRD/tasks/06 S5 test but history is src/stores/historyStore.ts (frontend). C3: macOS capture
  strategy leans on deprecated sharingType; G2 gate lacks ScreenCaptureKit in pass/fail and no Plan C.
  C4: lifecycle default (arrive-expanded-then-collapse) assumed by T4/T5 but PRD OQ-2 never ratified.
  C6: dedupe key concrete only in architecture (group ?? sender::title) - adopt everywhere.
  C7: T4 wires island into NotificationOverlay (top-right window) contradicting dedicated-window
  design - island would render in the WRONG window. Missing: a Playwright-harness-wiring task
  (earliest green gate), interruption-states only PARTIAL owned, FR-8 radius ranges unpinned.
  Settings backend NOT a conflict (serde JSON within G6 accepted set). T4/T5/T7 are L and bundle
  concerns - Wave V refutes one-PR claims.

[from:80-feasibility] [to:85-verify] Load-bearing assumptions to refute: A1 set_content_protected
  excludes the NSPanel under ScreenCaptureKit on macOS 14/15; A2 notch geometry reachable via
  objc2-app-kit public APIs and G1 spike is S-sized; A3 Model B pure-frontend-view claim (C1 crux:
  does the store mirror manager active+queued at rank time?); A4 --wait per-id event identity
  survives the merged list (exit codes 0/1/2); A5 T4/T5/T7 one-PR each; A6 ported morph loop rests
  without epsilon guard on sub-pixel measure noise.

# ---- Wave V harvest (orchestrator, post-V) ----

[from:85-verify] [to:90-synthesis] BINDING verdicts: A1 macOS 15+ SCK ignores sharingType/content
  protection (Apple DTS: no public API) - rescope capture story per-OS, G2 gate = live capture probe
  not read-back; A2 SURVIVES (objc2-app-kit 0.3.2 already in lock, public NSScreen APIs); A3 G10
  LETTER wins - manager owns rank/dedupe/cap/count, emits full snapshot, frontend dumb renderer
  (store provably diverges: W1-W4); A4 waiter-bearing notifications EXEMPT from dedupe merging
  (recommended (a)); A5 split T4/T5/T7 into a/b (13->16 tasks), T4 file list corrected for C7;
  A6 port keeps eps guard + measure rounding guard + dispose(), measure never per-frame.

[from:85-verify] [to:FINAL] Human product call required: accept honest macOS 15 capture scoping
  (guaranteed macOS<=14 + Windows; best-effort + documented limitation + optional auto-hide
  fallback on macOS 15+). This narrows a marketed differentiator.

# ---- Human decisions (orchestrator, post-FINAL, 2026-07-20) ----

[from:user] [to:ALL] OQ-1..5 RESOLVED: (1) capture = honest scoping, no auto-hide subsystem, macOS
  15+ shows "limited on this OS"; (2) lifecycle = arrive-expanded then auto-collapse to pill, --wait
  stays expanded; (3) field name = presentation (card|island, default card), CLI --presentation;
  (4) radii = mockup values (top 0-24/6, bottom 0-40/14); (5) Linux toggle disabled + honest label.
  Plan is fully unblocked for orchestrator-implementor. PRD amendments (6 items, 90-synthesis S5)
  should be applied as part of implementation kickoff.

# ---- Wave 0 harvest (orchestrator, implementation) ----
[from:G1] [to:T3] GO. Recipe in gates/G1-verdict.md: detect notch via safeAreaInsets().top>0 (aux
  rects are NSZeroRect on non-notch, never None/panic); MainThreadOnly via run_on_main_thread
  (panel.rs:196/229 pattern); iterate screens(mtm), never mainScreen(); measured 183pt built-in.
[from:G2] [to:T9] GO with tri-state: ON = Darwin<24 or Win>=19041; UNSUPPORTED = macOS15+/Linux/
  Win<19041 (still apply flag as defense-in-depth, never claim protection); UNKNOWN = undetectable,
  fail to "assume visible". Detect by OS version, NEVER sharingType read-back. On 26.5 CGWindowList
  is REMOVED; NSPanel default sharingType=readOnly; protect-before-show verified clean.
[from:T0] [to:T2] [to:T4a] Harness live: e2e/ + playwright.config.ts, snapshotPathTemplate keys by
  {projectName}/{platform}; port 5199 python http.server serves the mockup; add specs on this config.
  T2's dev-harness route should follow the same pattern.

[from:T4a-review] [to:T10] [to:T6] DISCOVERY: historyStore.prependEntry has ZERO production callers -
  history is an unwired display shell for card AND island (synthesis C2 described the intended, not
  actual, state). T10 (compat/docs) now OWNS wiring prependEntry at the shared ingest point for both
  presentations + the history-includes-island acceptance. T6 note: island-window store can be
  polluted by card broadcasts (MAX_VISIBLE starvation) until the Rust-owned snapshot lands - interim
  ingest filter applied in T4a fix pass.

[from:T4b-review] [to:T5b] [to:T6] Two LOW edge notes carried forward: (1) handoff jump possible only
  at floor(<72)/cap(>560) content edges - T5b's progress fixtures should stay in range or clamp;
  (2) the 560 spring cap is bypassed by auto-height at rest - T6 MUST enforce the Model B 560 list
  cap via its own overflow/scroll containment, not rely on the morph cap. Also: 20x settle test does
  not sample mid-flight frames - T5b's mid-morph interruption test should screenshot mid-flight.
