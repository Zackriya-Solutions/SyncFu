# PROJECT CONTEXT
Project: syncfu (notification-shell)
Repo: /Users/sujith/work/2026/reminders/notification-shell
Branch: main
Stack: Tauri v2 + Rust + React + TypeScript + Vite
Purpose: Universal overlay notification system for AI agents, skills, autonomous loops, and background processes.
Brand: syncfu.dev (open source)
User context: ADHD — needs push-based notifications, doesn't go back to read terminal output.

# COMPLETED WORK

## Phase 0: Planning & Documentation
- [x] PLAN.md — full architecture, project structure, all 6 phases, verification plan
- [x] README.md — open source readme with 15+ use case categories, API docs, CLI examples, integrations
- [x] ARCHITECTURE-COMPARISON.md — analysis of 4 reference apps (Handy, Cap, Meetily, tauri-plugin-liquid-glass)
- [x] Confirmed decisions: HTTP REST :9868, WebSocket :9869, NSPanel on macOS, WebviewWindow on Windows

## Phase 1: Scaffold + TDD Foundation (COMPLETE)
- [x] Tauri v2 project scaffolded (Cargo.toml, tauri.conf.json, build.rs)
- [x] React + Vite frontend with TypeScript
- [x] Vitest test setup with Tauri API mocks
- [x] .gitignore, placeholder icons

### Rust backend
- [x] notification/types.rs — NotificationPayload, Action, ProgressInfo, Priority, Timeout, HistoryEntry (10 tests)
- [x] notification/manager.rs — NotificationManager with add/dismiss/update/dismissAll/list (11 tests)
- [x] lib.rs — Tauri commands, overlay panel creation in setup(), panic hook, main thread dispatch
- [x] tray/menu.rs — System tray with Open, Clear All, Pause, Quit (6 tests)
- [x] server/http.rs — axum HTTP server on port 9868 (13 tests, includes action endpoint)
- [x] server/webhook.rs — webhook callback client, 5s timeout, camelCase payloads (4 tests)
- [x] overlay/panel.rs — NSPanel (macOS) / WebviewWindow (Windows) with position calc (7 tests)
- [x] tauri-nspanel integration — non-activating, joins all Spaces, proper z-order
- [x] Main thread dispatch for all panel show/hide ops (prevents AppKit crashes from tokio threads)
- [x] Dynamic panel sizing — starts 10px, frontend resizes to fit content
- [x] cargo check passes, cargo test passes (51 tests)

### Frontend
- [x] types/notification.ts — mirrors Rust types + icon, font fields
- [x] stores/notificationStore.ts — Zustand store (13 tests)
- [x] stores/historyStore.ts — Zustand store (13 tests)
- [x] App.tsx — multi-window router (2 tests)
- [x] components/overlay/NotificationOverlay.tsx — transparent container, Tauri events, dynamic resize, action callbacks (16 tests)
- [x] components/overlay/NotificationCard.tsx — glass card with icons, fonts, countdown, dismiss animation (9 tests)
- [x] components/overlay/NotificationIcon.tsx — dynamic Lucide icon by name (kebab/pascal/lowercase)
- [x] components/overlay/RelativeTime.tsx — "just now" / "5m ago" / "1h ago" with smart tick intervals
- [x] hooks/useNotifications.ts — Tauri event subscriptions (11 tests)
- [x] hooks/useGoogleFont.ts — dynamically loads Google Fonts via <link> injection, deduplicates
- [x] components/app/MainApp.tsx, HistoryView.tsx (4 tests)
- [x] __mocks__/ — Tauri API, autostart, dialog mocks
- [x] pnpm test passes (68 tests)

### Overlay Design: Liquid Glass (Meetily-inspired)
- [x] 9-layer reflex shadow stack (4 light + 3 dark + 2 drop shadows)
- [x] backdrop-filter: blur(40px) saturate(1.8) with 96% opacity dark bg
- [x] 18px border-radius, 12px button radius
- [x] Light/dark theme: auto via prefers-color-scheme + per-card override (.light/.dark class)
- [x] Priority accent: 0.5px top border (green/blue/orange/red)
- [x] Critical pulsing glow: Siri-style ::before/::after with animated opacity, blurred outer halo
- [x] Slide-in animation: translateX(40px) scale(0.96) with spring easing
- [x] Slide-out dismiss: translateX(60px) scale(0.95), 280ms, pointer-events disabled
- [x] Auto-dismiss countdown bar: priority-colored, shrinks over timeout, pauses on hover
  - Low: 6s, Normal: 8s, High: 12s, Critical: never
- [x] Lucide icons: `icon` field in payload → dynamic icon render (any of 1400+ icons)
- [x] Priority-tinted icon containers: colored bg/border matching priority accent
- [x] Google Fonts: `font` field in payload → dynamically loaded from CDN, applied per-card
- [x] Relative timestamps: "just now" → "5m ago" → "1h ago" → "3d ago" (smart tick intervals)
- [x] Typography: SF Mono for sender/timestamp, SF Pro Display for content
- [x] Subtle SVG noise grain at 3% opacity for perceived depth
- [x] Close button: 18px circular, SVG cross, appears on hover
- [x] Action buttons: glass-style, 0.5px border, 28px height, font-weight 500
- [x] Body text: 62% opacity (bumped from 50% for readability)

### Test script
- [x] scripts/fire.py — comprehensive test notification sender
  - --all (each priority with icons), --burst N, --progress, --actions, --critical
  - --icons (5 different icon types), --fonts (4 different Google Fonts)
  - --webhook (starts listener on :9870, sends notification with callback_url)

### Git history (this phase)
297c06c fix: start panel at 10px height, frontend resizes to fit content
136fe13 fix: use direct window import for setSize, add window subpath mock
c25295b fix: dynamically resize panel window to fit notification content
af51efe fix: proper webview transparency using background_color + data attribute
fb2b9eb fix: add transparent(true) to webview builder inside NSPanel
a7171e7 fix: dismiss/dismissAll call backend to sync manager state
c6be638 fix: set transparent background on overlay webview
d1ded03 fix: dispatch NSPanel show/hide to main thread, add panic hook
cb2e0c0 feat: add tauri-nspanel for macOS NSPanel notification overlay
d9d56a8 docs: add architecture comparison of notification overlay strategies
d783e99 refactor: replace fullscreen overlay with positioned panel window

# TODO LIST

## Phase 3: Click-Through — NOT STARTED
- [ ] CSS pointer-events: none on overlay body, auto on cards
- [ ] Rust-side mouse position polling (macOS: CGEvent, Windows: GetCursorPos)
- [ ] setIgnoreCursorEvents toggle logic

## Phase 4 Remaining: WebSocket + CLI
- [x] axum HTTP server on port 9868 — all REST endpoints (13 tests)
- [x] Webhook callbacks: POST /notify/{id}/action fires to callback_url, dismisses notification
- [x] action_callback Tauri command: frontend action buttons → webhook + dismiss
- [x] fire.py --webhook test mode: starts listener on :9870, verified end-to-end
- [ ] tokio-tungstenite WebSocket server on port 9869
- [ ] CLI binary (cli/Cargo.toml, clap + reqwest)

## Phase 4.5: Test UI
- [ ] Sidebar navigation (History / Tester tabs)
- [ ] NotificationTester component with form fields
- [ ] Live preview, Send button, Copy curl, Quick templates

## Phase 5: Rich Features
- [x] Action buttons + callback dispatch (POST to callback_url) — DONE
- [ ] Markdown body rendering (react-markdown)
- [ ] Notification grouping (collapse/expand)
- [ ] Sound playback (rodio)

## Phase 6: History + Settings + Polish
- [ ] SQLite history (sqlx)
- [ ] History UI in main app window
- [ ] Settings view
- [ ] Multi-monitor support
- [ ] Cross-platform testing

# KNOWN ISSUES
- Placeholder icons are solid blue squares (need proper bell icons)
- pnpm build scripts (esbuild) not approved — `pnpm approve-builds` needed interactively
- Tray icon states (has-notifications, paused) not implemented
- Transparency + dynamic resize need visual verification after Rust rebuild

# CURRENT STATUS
119 tests passing (68 frontend + 51 Rust). Overlay is fully functional with Liquid Glass design,
Lucide icons, Google Fonts, auto-dismiss, critical glow, dismiss animations, relative timestamps,
and webhook callbacks. Action buttons fire HTTP POST to callback_url and dismiss the notification.
Next: WebSocket server, CLI, or Test UI.

# OBSERVATIONS & DECISIONS
- NSPanel is the correct primitive for macOS floating utility panels (non-activating, joins all Spaces)
- Windows uses WebviewWindow with always_on_top + focused(false) (Meetily pattern), Linux deferred
- Fullscreen transparent overlay abandoned — every production Tauri app uses small positioned windows
- panel.show() from tokio thread crashes AppKit — all panel ops dispatched to main thread
- height: 100% on overlay-root caused window to never shrink — fixed with auto height + stack measurement
- color-mix() for glass works but 12% opacity too transparent on busy backgrounds — bumped to 96%
- CSS @property for --pulse-opacity enables smooth Siri-style glow animation
- Google Fonts loaded via dynamic <link> injection with display=swap — deduplicates across cards

# ORCHESTRATOR RUN: dynamic-island (2026-07-21)
Integration branch: feat/dynamic-island (off main). Plan: tasks/dynamic-island/FINAL-plan.md;
authoritative specs 90-synthesis.md S3. 17 tasks. User decisions OQ-1..5 resolved (honest capture
scoping / arrive-expanded lifecycle / presentation field / mockup radii / Linux toggle disabled).

## Wave 0 (parallel) - DISPATCHED 2026-07-21
- [ ] T0-HARNESS -> feat/island-t0-playwright-harness (wt: ../syncfu-wt-island/t0)
- [ ] G1 notch spike -> spike/island-g1-notch-geometry (wt: ../syncfu-wt-island/g1)
- [ ] G2 capture spike -> spike/island-g2-capture-exclusion (wt: ../syncfu-wt-island/g2)
- [ ] T1 presentation field -> feat/island-t1-presentation-field (wt: ../syncfu-wt-island/t1)
Decisions: plan package + TODO drafts committed to integration branch [skip ci]; workers share
CARGO_TARGET_DIR with main worktree for cache reuse; G2 live probe runs on macOS 26.5 (post-15 SCK
world - newest data point for the A1 finding).
- G1 RETURNED: GO. notchWidth=183pt measured (26.5); NSZeroRect (not None) on non-notch; MainThreadOnly;
  iterate screens() not mainScreen(). Commit 0bcaecc. Verdict: tasks/dynamic-island/gates/G1-verdict.md
- G2 RETURNED: GO for T9 honest tri-state. 26.5: CGWindowList REMOVED from SDK, all capture funnels
  through TCC-gated SCK; NSPanel defaults sharingType=readOnly; set_content_protected applies clean
  pre-show. SCK live cell UNKNOWN-by-observation (no TCC grant for agent process) - documented
  inference stands (captures on 15+). Detect by OS version, never read-back. Commit dccc940.
- T0 RETURNED: harness green. 42 baselines (webkit-darwin + chromium-darwin, never mixed), 30 tests
  passed twice, corrupt-baseline fails build, tsc+vitest green. Commit 4dfd211.
- DEVIATION: remote push + gh pr create denied by permission classifier. Phase 4 adapted: second-eyes
  review agents + LOCAL no-ff merges into feat/dynamic-island; all pushes/PRs deferred to final-PR
  moment (user present). Rationale: review rigor + merge authority preserved; only the GitHub
  ceremony for intermediate PRs is deferred. Worktrees preserved regardless.
- T1 RETURNED: all gates green (143 Rust + 72 vitest); wire-level proof --presentation island survives
  HTTP boundary; client.rs skip justified. Commit b5f3f5d. Second-eyes review dispatched.
- T0 REVIEW: CLEAN (all invariants verified live incl corrupt-baseline failure). MERGED into
  feat/dynamic-island (no-ff). Latent host assumption noted: webServer uses python3 (fine on mac CI).
- T1 REVIEW: CLEAN (serde defaults verified per boundary; wire-body test spawns real binary; guards
  G8/G9/G12 hold; client.rs skip validated). MERGED into feat/dynamic-island.
- Wave 0 COMPLETE: 4/4 tasks merged. Dispatching T3 (island window; consumes G1 recipe + T1 field).
- T2 RETURNED: port complete (notchPath/spring/styleVars + Vite harness route). Drift decisions:
  margins +4/+5/+2 canonical; float expanded radius 24 (D3 wins over mountStatic 22). 92 vitest +
  40 playwright green x3. Worker self-caught 4 findings (wrong reduced-motion test invariant,
  Vite IPv4 bind, dead prefersReducedMotion helper, em dashes). Commit 4b42e32. Review dispatched.
- T3 RETURNED: island window + routing complete. Envelope 600x560 (range maxima); primary-monitor
  targeting when notched (multi-notched-display deferral documented); card path byte-identical via
  route_show_and_emit; island adds scoped emit_to("island") to prevent overlay state leak (FR-3).
  90+22+32+12 tests green. Worker simplified own API (click-through), kept reflow_island per spec.
  Commit 031e12a. Review dispatched.
- T2 REVIEW: CLEAN (geometry byte-exact vs mockup; A6 eps/rounding/dispose verified; 27-entry map
  identical; 40 playwright green incl T0 specs on the dual-webServer config). MERGED. Reviewer
  observation: harness self-baselines (not pixel-diffed vs mockup shots) - correct call, golden
  strings prove geometry; noted for the record.
- T3 REVIEW: FINDINGS (1 MEDIUM: tray clear-all missed hide_island - third dismiss-all path; 2 LOW:
  label single-source, comment em dashes). All fixed by orchestrator (mechanical), gates re-green
  (90+22+32+12). MERGED. Reviewer observation carried to T4a: confirm 560px expanded list does not
  clip under notch overhang (envelope has no top slack).
- DEPENDENCY RECONCILIATION: FINAL-plan index table drifted from authoritative synthesis specs.
  T7a depends T4a (not just T3; C5 lock); T9 depends G2+T7a (not G2+T3 - needs the toggle).
  Synthesis section 3 is authoritative (FINAL-plan says so itself). No parallel dispatch of T7a/T9
  yet; critical path stays T4a -> T4b -> T5a -> T5b with T7a joining after T4a.
- T4a RETURNED: first visible island (IslandOverlay/Island/Compact/Expanded + App.tsx routing +
  overlay exclusion filter). 107 vitest + 58 playwright green x2. Self-caught circular content
  measurement bug (content now normal-flow, SVG absolute). Notch-overhang check: grows downward,
  no self-clip, envelope cap asserted; placement concern owned by T3/T5b. Commit f74e694.
  Review dispatched.
- T4a REVIEW: FINDINGS (2 MEDIUM + 1 LOW). F1: cardBg override out of CSS-var scope for the SVG
  path (empirically proven; masked by near-identical fixture color) -> styleVars moves to island
  root. F2: invariant-e comment FALSE - history is unwired for ALL presentations (prependEntry has
  zero production callers); pre-existing product gap, now owned by T10; honest comment + test rename
  in T4a. F3: island-window store starvation by card broadcasts (interim until T6 snapshot) ->
  ingest filter. Fix-pass worker dispatched. DISCOVERY logged to thought ledger.
- T4a FIX PASS: F1 styleVars to island root (path fill now provably overridable, green fixture);
  F2 honest comments/test (history unwired truth); F3 ingestFilter predicate on useNotifications
  (island window immune to card-broadcast starvation, 6-cards+1-island regression test). 108+58x2
  green. Commit e15892b. MERGED. 7/17.
- Dispatching T4b (morph engine) + T7a (settings backend) in parallel.
- T7a RETURNED: settings backend complete (settings.rs + IPC + TS mirror/store). Playground-exact
  schema incl {"island":{...}} wrapper; opacity stored 0.0-1.0 (documented decision - UI percent is
  T7b domain); island.rs capture/interactive APIs parameterized. 105 lib + 112 frontend green.
  Commit 1fc3ad2. Review dispatched. T4b still running.
- T7a REVIEW: CLEAN (defaults field-exact vs mockup; clamps both paths; atomic same-fs rename;
  conditional side effects verified; G6/G12 hold). MERGED. 8/17.
- T9 now unblocked (G2+T7a); dispatching in parallel with T4b.
- T9 RETURNED: capture-status derivation shipped (pure fn + injected facts; NSProcessInfo product
  major; cmd /C ver fail-safe; Linux constant). Matrix implemented per OQ-1/OQ-5; flag applied
  unconditionally as defense-in-depth; loop-test asserts macOS 15..40 never ON. 11 Rust + 3 TS
  tests. Commit dc26d39. Review dispatched. T4b still running.
- T9 REVIEW: CLEAN (invariant b structurally impossible to break - product-major compared directly,
  no Darwin conversion; fail-safe verified branch-by-branch; ver-parse fragile-but-fail-safe
  accepted; 4th BestEffort state justified as the OQ-1 honesty carrier). MERGED. 9/17.
- Waiting on T4b (last blocker for the T5a/T7b/T8 fan-out).
- T4b RETURNED: morph engine (islandMorph.ts controller + thin React host). IslandHandle
  {expand,collapse,state} for T5; __islandSettled + __islandFrames hooks; arrive-expanded ->
  2600ms hold -> auto-collapse. Self-caught: 0.02px baseline drift (content-driven height at rest),
  first-frame dip (sync measure in setTargets), reduced-motion honesty (1000/100 is ~62 frames, now
  true snap). 114 vitest + 62 playwright x2. Commit 4258639. Review dispatched.
- T4b REVIEW: CLEAN (A6 verified line-by-line; handoff re-derived seamless in range; remount
  semantics correct). 2 LOW edge notes -> thought ledger for T5b/T6. MERGED. 10/17.
- FAN-OUT: dispatching T5a + T7b + T8 in parallel (widest wave).
- T7b RETURNED: settings UI (sidebar section beside History; 13 controls + presets + honest capture
  status). Worker removed its own optimistic-reconcile race in review. islandMorph gained
  configure()/applySettings(). 133 vitest + 72 playwright x2. Documented note: first island
  appearance with non-default settings briefly animates from defaults (async get). Commit ff87a87.
  Review dispatched. T5a + T8 still running.
- DISK EMERGENCY: volume hit 100% (137MB free) mid-run, caught by T7b reviewer's ENOSPC. Freed
  8.8GiB via cargo clean (shared target + stale 2026 copy) - sanctioned tooling only, no rm.
  Workers pay one-time rebuild. Disk now 61%.
- T7b REVIEW: no blockers (2 fidelity nits accepted: lock-note caption replaced by live status -
  arguably better per T9; hex input dropped for native picker - ponytail-favorable). Playwright
  step unverifiable in review due to ENOSPC but worker ran it green twice pre-exhaustion. MERGED.
  11/17. Note for T8 merge: backend/store-level notch+bottom-center rejection guard suggested when
  T8 activates positioning.
- T8 RETURNED: appearance + positions complete. Elegant calls: bottom-center = pure CSS flex-end
  (zero JS anchor awareness); mirror = shared buildNotch(fy) y-flip; auto-appearance sync seed fix
  (no dark flash). 6 anchor tests + 130 vitest + 92 playwright x2. Commit 88cc1eb. Review dispatched
  WITH trial-merge check vs T7b-bearing integration (both touched Island.tsx/islandMorph.ts).
- T8 MERGE: executed per reviewer conflict map (import unions, ordered creation effect
  configure->snap->setSurface, both live effects kept, silent duplicate `settings` declaration
  resolved). Applied reviewer's LOW polish: light fills now honor --di-surface-opacity (2 test
  assertions updated to match). tsc + 142 vitest green; playwright + cargo running.
- T5a WORKER STALLED near token guard waiting on post-clean cold cargo build (~340 lines
  near-complete uncommitted). Resumed via SendMessage with economy finish order: gates -> commit.
- T8 merge committed (573f182) after tsc+vitest green; CAUGHT a masked gate: playwright had not
  actually run on the merged main tree (ERR_MODULE_NOT_FOUND - @playwright/test missing from main
  repo node_modules, pipe swallowed the exit code). pnpm install + real playwright run now in
  flight; will fix forward if red.
- T8 merged tree FULLY verified: tsc + 142 vitest + 91 playwright (real exit 0) + cargo check.
  12/17 merged. Awaiting T5a (resumed) - last blocker for T5b + T6.
- T5a REVIEW: FINDINGS adjudicated by orchestrator: F1 keep the latent-card-bug fix (critical
  previously auto-dismissed at 8s via `?? 8000` on null - violated locked priority contract);
  VERBATIM comment corrected to honest deliberate-change documentation. F2 folded into T6:
  backend hasWaiter enrichment; suppression scoped to waiter-backed decisions; non-wait actionables
  follow card auto-dismiss; island unanswered --wait -> exit 2 RATIFIED (matches OQ-2); invariant-b
  rewording queued as PRD amendment for T10. F3 noted - T6 owns closing A4.
- T5a MERGE: conflict map executed (props union, core.invoke rewrite, css union WITH the extra
  @keyframes closing brace - the silent trap). tsc + 155 vitest green; playwright verifying.
- DISK BLOCKER RESOLVED (user): freed to 30Gi; orchestrator removed the 13 merged worktrees per
  user instruction (branches preserved; t5b kept until merged). T5b review re-dispatched.
- T5b REVIEW: core CLEAN (all invariants hold; card notification:update wiring adjudicated as
  spec-aligned latent-bug fix - PR note queued). 3 mechanical fixes applied by orchestrator (atomic
  mid-flight assertion, light-mode track visibility, mini-ring r=9). 175/175 green. MERGED (14/17).
  Reviewer's baseline-environment observation noted for T11/CI (visual baselines are
  machine-sensitive; canonical runner needed).
- T6 Model B dispatching (last feature task).
- STRAY PROCESSES: user reported many syncfu instances. Root cause: review agents' cargo test runs
  hit bash timeouts which kill the shell but NOT the test process tree - two generations of the CLI
  integration binary + their --wait children were stuck against dead wiremock ports (NOT the user's
  live daemon; no screen impact). Killed by PID; user's /Applications/syncfu.app untouched. NOTE for
  T11/RELEASE-CHECKLIST: integration tests spawning --wait CLIs should carry hard process timeouts;
  agent gate runs should avoid backgrounding cargo test without cleanup.
- T6 REVIEW: 1 MEDIUM (A4 emit-freshness hole: handle_wait subscribe never re-emitted snapshot ->
  add->subscribe race leaves waiter merged away, wrong exit 2) + 1 LOW (action emit asymmetry) +
  1 NIT (vestigial island notification:add - kept). Orchestrator applied both fixes per reviewer
  spec; 134 Rust + 182 vitest green. MERGED (15/17). Ponytail: ~90-line IslandGroup duplication
  adjudicated as forced isolation cost; post-merge useIslandMorph extraction noted as follow-up.
- T10 dispatching (docs + history wiring + PRD amendments).
- T10 RETURNED: docs (README island section + honest capture matrix + CHEATSHEET), history wiring
  REDESIGNED for post-T6 reality (backend history:add broadcast at 3 send sites + main-window
  useHistoryIngest + idempotent prependEntry - the TODO's useNotifications premise was stale),
  6 PRD amendments + invariant-b applied with changelog. Worker self-caught two doc-vs-code drifts
  (unknown-presentation 422 not fallback; radius ranges 0-24/0-40 shipped vs synthesis 0-20/0-30).
  FLAG adjudicated by orchestrator: PRD FR-1 amended to strict-validation semantics (consistent
  with priority; loud failure beats wrong presentation). 193 vitest + 36 playwright green.
  Commits e402342 + FR-1 amendment. Review dispatched (docs-vs-code audit).
- T10 REVIEW: CLEAN (docs-vs-code audit table fully verified incl honesty gate; history wiring at
  exactly 3 emit sites; PRD amendments confined to amended sections). NIT (changelog count
  phrasing) accepted as-is. MERGED (16/17). Tooling discovery: app + CLI binaries COLLIDE on the
  name `syncfu` in the shared CARGO_TARGET_DIR (app wins) - T11 must invoke the CLI via
  `cargo run -p syncfu-cli` or CARGO_BIN_EXE, and the release checklist gets a note. Reviewer's
  stray app launch killed.
- T11 dispatching - the final task.
- T11 RETURNED: journey 20/20 green against the REAL stack (real CLI x real http/manager/waiters on
  ephemeral port; app spawn blocked by hardcoded :9868 held by user's live app - degraded honestly
  via journey_server.rs example harness reusing production APIs). All exit codes + A4 3-waiter
  routing + Model B ranking + card regression PASS. Zero functional defects on first live run.
  Gaps reported: F1 no HTTP snapshot surface (probe route used); F2 SYNCFU_PORT env override would
  make the app CI-spawnable. RELEASE-CHECKLIST.md written. Commit 78a02f0. Final review dispatched.
- T11 REVIEW: CLEAN. Reviewer independently RE-RAN the journey (20/20) and proved the CLI hit the
  ephemeral harness (hasWaiter evidence), verified example excluded from workspace check, cleanup
  clean, user's app untouched. 2 LOW advisories applied by orchestrator (R-SSE carried in checklist
  + F1/F2 testability recommendations). MERGED. 17/17 COMPLETE.

## RUN COMPLETE (2026-07-21)
All 17 tasks merged into feat/dynamic-island. 17 second-eyes reviews (every finding fixed and
reverified). Final full-gate sweep running; final PR next (never auto-merged; user reviews).
Worktrees preserved: t5b, t6, t10, t11 (earlier 13 removed on user instruction).
- FINAL SWEEP 4-failure POSTMORTEM: my demo-app build (cargo build -p syncfu for the user's sample
  island) overwrote target/debug/syncfu WHILE the sweep's CLI integration tests spawned that path -
  the documented binary-name collision live-firing. Isolated re-runs: app crate 136 green (even with
  the app holding :9868), cli crate 69 green. Zero code defects; PR #5 stands. The collision +
  no-concurrent-builds rule is already in RELEASE-CHECKLIST.md.
- DEMO: new app running on :9868 (user-consented swap from production app); sample island decision
  notification delivered. Old app relaunch: open /Applications/syncfu.app
- HARDWARE ITERATION 2 (user on real notch): design ratified as UNDER-NOTCH second-notch pill
  (supersedes T12 wings) + HOVER-REVEAL (pill hidden when collapsed; hovering the physical notch
  region slides it out; grace conceal; arrival announce unchanged; float mode keeps visible pill).
  Hover root cause found by code-reading: hover.rs tick() mixes CGEvent POINTS with outer_position
  PHYSICAL pixels - containment never true on 2x Retina; unit tests encoded the author's own wrong
  convention. T13 worker dispatched: redesign + verified-convention fix + live probe evidence.
  Pre-existing latent: get_cursor_monitor_info same unit mix (flagged, out of scope).
- HARDWARE ITERATION 3 (user): T13 hidden-when-collapsed lost the ambient "something is happening"
  signal. Ratified synthesis: collapsed = AMBIENT WINGS indicator (T12 shape: cutout+2x24px wings,
  cutout height, accent dot / mini progress-ring in the right wing, no other content) -> hover
  reveals the T13 under-notch pill -> expanded unchanged. Hot-zone extends over the wings. T14
  worker dispatched (also folds in T13-review comment nits).
- T14 REVIEW: CLEAN (state matrix walked; ambient dot geometry re-derived into the visible wing;
  hot-zone tests proven non-vacuous; controlled-removal safe). 3 non-blocking follow-ups QUEUED for
  the next Rust touch (avoid restarting the app mid-user-test): LOW pin AMBIENT_WING const in Rust
  tests (drift guard), 2 NITs (dead param, redundant helper). Scope observation: IslandGroup (N>1)
  has no ambient/hover-reveal - conscious boundary, surfaced to user for a design call.
- T15 REVIEW: CLEAN (critical-dismiss chain traced end-to-end; clear-all dedupe reasoning verified
  against manager.rs; single-instance registration verified first-in-chain). 2 LOWs: css
  pointer-events hardening APPLIED (HMR); single-instance callback should open_main_window
  (creates-if-absent) - QUEUED for next Rust touch. Observation: clear-all's clears-cards-too side
  effect is dev-documented only.
- HARDWARE ITERATION 5 (user): REGRESSION vs original card - island does not follow the active
  screen (T3's documented multi-display deferral now invalidated by real dual-display use) "and
  all" -> T17 dispatched: cursor-monitor targeting with per-display notch-vs-float auto-selection,
  the T13-flagged points-vs-physical get_cursor_monitor_info fix (also corrects the CARD's
  follow-me on Retina), plus a systematic card-vs-island parity audit (hover-pause, fonts, sound,
  markdown, +anything found). T16 docs runs in parallel (T17 barred from doc files; doc-impact
  list returned instead).
- T17 MERGED: island follows the cursor's monitor (per-display auto notch/float; float forces
  everywhere; position honored only in explicit float). Retina monitor-picker fixed (logical
  end-to-end - ALSO fixes the card's pre-existing follow bug). Hover pauses auto-dismiss
  (island:hover event). Parity audit: fonts no-gap, countdown-bar no-gap (card never paused the
  bar), sound never-played-for-either (reported), markdown plain-for-both + unused deps (reported),
  group hover-pause + group fonts (reported minor). 158+32 Rust, 216 vitest green; playwright
  deferred (user's live session owns 1420).
- T16 MERGED: README/CHEATSHEET/skill files/docs/island.md/examples (3 of 4 run live). FLAG: the
  user's INSTALLED CLI 0.3.0 predates --presentation - republish with the release (checklist).
- Doc-impact deltas applied: docs/island.md gains display-targeting + window-events sections.
- T19 MERGED (1334c1f): waiter drop-guard (same_channel + receiver_count identity/liveness - no
  generation counter needed; early-return leak also closed; snapshot re-emit on cleanup) + group
  hitbox holes closed (accordion + row-count-while-open; empirically proven delta-0 before fix)
  + 4 T18-review nits folded. 170 Rust + 225 vitest. Iteration 8 complete pending user re-run of
  the 6-step reproduction. Review in flight.
- T19 REVIEW: CLEAN - every drop-guard interleaving independently walked and verified sound
  (incl. drop-vs-new-subscribe races both orderings); disconnect test proven CI-safe; T18 nits
  confirmed folded. 3 minor notes queued as follow-ups: spawn-guard belt-and-suspenders,
  end-to-end resend-merges A4 assertion, harmless double hitbox report on expand. Also on record:
  pre-existing subscribe-vs-check microsecond race sends synthetic Dismissed (predates T19,
  documented). Iteration 8 closed pending user re-run.
